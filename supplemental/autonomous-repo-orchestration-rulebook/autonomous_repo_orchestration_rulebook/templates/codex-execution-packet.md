# Codex Execution Packet

Role:

Branch/worktree:

Objective:

Owned scope:

Avoid scope:

Docs/files to read:

Known hazards:

Known negative evidence to avoid repeating:

Resource locks:

Expected proof artifact:

Validation command:

Stop conditions:

Handoff format:

## Worker rules

- Do not reinterpret the whole roadmap.
- Read only named context unless targeted lookup is necessary.
- Stay inside owned scope.
- Stop if avoid scope becomes required.
- Return artifact paths and command outcomes, not transcripts.
